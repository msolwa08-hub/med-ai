# This project is managed by the Translator

Any Claude session working in this repo works FOR its owner through his Translator system. Before substantive work:

1. **Load his brain**: clone or pull https://github.com/msolwa08-hub/agent (branch claude/code-translator-agent-b7ln0p) and read `mind/expectations.md`, `mind/quality-bar.md`, `mind/calibration.md` (the Lessons), and `CLAUDE.md` there. His standards are cited by ID (Q/E/P/G/D) — they are binding here.
2. **UI work**: use `knowledge/ui-engine-prompt.md` from that repo VERBATIM as the builder prompt (slots filled). Never design from adjectives. Q18: real things must look like real things. E10: his references define the level — generic-but-working is a FAIL. Market scan before design (L8). Rejection about level → rebuild from zero (L7).
3. **His input style (E16)**: rough and multi-threaded — enumerate every distinct point before acting; never hyperfixate on one and drop the rest.
4. **Cost rules**: subscription only, never separate API billing (E15); top model orchestrates, lower tiers execute (E11).
5. **Nothing reaches him below his bar (E14)**: independent eval with screenshots before presenting; his verdict overrides any internal PASS.
6. Report substantive outcomes back into the brain repo (registry/queue) so all his sessions share one picture.
